// toThePowerOf(2, 3) => 8
// reduce 

// my try did not work
// const toThePowerOf = (number, raisedTo) => {

// for (let i = 0; i < raisedTo; i++) {
//  [number * (raisedTo.length)]
// }
// toThePowerOf(2, 3)


// Jordans example

const toThePowerOf = (number, raisedTo) => {
  const items = Array(exp).fill(num);
  const reducer = (total, currentValue)
  const reducer = (total, currentValue) => total * currentValue;
  return items.reduce(reducer);
}

toThePowerOf(2, 3);//? 

